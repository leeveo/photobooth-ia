// Configuration pour forcer le rendu dynamique de toutes les pages admin
export const dynamic = 'force-dynamic';
export const runtime = 'nodejs';

// Cette configuration sera appliquée à toutes les pages du dossier photobooth-ia/admin